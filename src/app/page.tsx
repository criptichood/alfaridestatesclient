// This file is intentionally left blank.
// The new entrypoint for the homepage is src/app/[locale]/page.tsx
// The middleware will redirect users from / to /en (or another language).
export default function Page() {
  return null;
}
